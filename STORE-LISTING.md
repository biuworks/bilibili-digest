# 商店说明

Chrome 应用商店与 Edge 加载项共用这一份，改动请两边同步。
这个文件不进安装包（`scripts/package.sh` 走白名单），只作为提交时的稿件来源。

**不要在说明里罗列模型服务商的名字。** 2026 年 8 月的一次提交列了九个厂商名，
被 Chrome 应用商店判为「关键字垃圾内容」打回，政策原文禁止「过多或不相关的元数据」。
用户在这一步真正需要知道的只有三件事：要自备密钥、地址自己填、本地模型也能用；
具体支持哪些服务，装完在设置页的下拉里一眼就看到，不必写进商店文案。
同理，截图和宣传图里也不要出现服务商的 logo。

---

## 详细说明

```text
把 B 站视频变成可以读、可以查、可以记的学习材料。在播放页旁边打开侧边栏，
字幕、概览和笔记都在手边，不用离开视频。

【字幕阅读】
字幕取自 B 站官方接口，按语义分段排版。当前句跟着播放高亮，点任意一句
跳到对应时间点，滚开了有「回到当前句」浮标带你回来。支持全文搜索、
一键复制和导出 TXT。

【双语对照】
原文、译文、双语三种视图随时切换。翻译是双向的：中文字幕译成英文，
外文字幕译成中文，方向按字幕语种自动判断。

【顺句】
B 站的 AI 字幕没有标点、同音错别字多。开启后由模型补标点、改错别字，
只做可读性修复，不改措辞，也不动时间轴。

【AI 概览】
通读整篇字幕，产出带时间戳的章节和金句，点章节直接跳转。长视频会自动
切块并发生成再合并，部分块失败不影响已完成的部分。

【划词解释】
选中不懂的术语，结合前后文用大白话讲清楚，解释浮在选中的文字旁边。

【笔记】
播放时按 n 或点播放器上的按钮，记下当前时间点。模型会把那句字幕整理成
通顺的一句话，卡片支持回放、复制和带时间戳的链接。

【关于模型服务】
本扩展没有服务器，也不代收任何费用。你需要在设置页填入自己的模型服务
地址和密钥，扩展直接与该地址通信。地址完全由你决定，填本机地址时数据
不出设备。可选的服务在设置页的下拉列表里提供。

【隐私】
不收集身份信息，不埋点，不接入分析或广告。密钥、字幕缓存和笔记只写在
本机的扩展存储里，卸载即清除。字幕功能只访问 B 站域名；模型服务的域名
在你保存设置时单独申请授权，可随时收回。
```

## 简短说明

与 `manifest.json` 的 `description` 保持一致（商店对该字段有 132 字符硬上限）。

## 提交时填的「认证说明」

AI 功能要密钥才跑得起来，不给审核员一把钥匙，多半会以「功能无法验证」被退。
按下面这个形状填，占位的地方每次提交前换成当次可用的值：

```text
本扩展不提供模型服务，需要用户自备 API 密钥，因此附上一把测试用的。

1. 安装后会自动打开设置页，服务商选「<预设名>」，密钥填：<临时密钥>
2. 打开这条带字幕的视频：https://www.bilibili.com/video/<BV 号>
3. 点工具栏上的扩展图标打开侧边栏，字幕会自动加载（这一步不需要密钥）
4. 切到「概览」标签点「生成概览」，即可验证需要密钥的 AI 功能

字幕来自 B 站官方接口。部分视频的字幕需要 B 站登录态才可见，上面这条
无需登录即可取到字幕。测试密钥在审核期间有效，通过后会被吊销。
```
